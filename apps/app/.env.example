# SilaaiSaaS App — Environment Variables
# Copy this file to .env.local and fill in real values.
# NEVER commit .env.local to git.

# Backend API base URL (no trailing slash)
VITE_API_BASE_URL=http://localhost:8080/api/v1
